# Workflow: Git Undo

Bridge workflow for `/undo`.

Read `.ai/commands/undo.md` and execute the git-guardian undo protocol. Prefer
reverting only the last AI-owned change and never discard unrelated user work.
