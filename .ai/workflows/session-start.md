# Workflow: Session Start

Bridge workflow for `/session-start`.

Read `.ai/commands/session-start.md`, then load memory, context, boundaries,
state, and global skills as instructed by `.ai/boot/BOOT.md`.
