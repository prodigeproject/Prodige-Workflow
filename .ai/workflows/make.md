# Workflow: Make

Compatibility bridge for `/make`.

Immediately route to `workflows/magic-orchestrator.md` with the same user task.
Do not maintain separate logic for `/make`; Prodige has one main workflow.
