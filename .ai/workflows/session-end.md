# Workflow: Session End

Bridge workflow for `/session-end`.

Read `.ai/commands/session-end.md`, update Memory Bank state, maintain the memory
index, record outcome data, and compact large memory files when thresholds are met.
