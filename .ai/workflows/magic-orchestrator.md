# Workflow: Magic Orchestrator

Bridge workflow for `/magic`.

Load `.ai/agents/magic-orchestrator.md` and execute its five-phase routing
protocol. This file exists so registry routing has a concrete workflow target.
