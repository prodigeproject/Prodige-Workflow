# Workflow: Verification

Bridge workflow for `/verify`.

Read `.ai/commands/verify.md` and execute the verification-runner protocol:
tests, lint, types, build, security/dependency checks where available, and clear
evidence before claiming completion.
