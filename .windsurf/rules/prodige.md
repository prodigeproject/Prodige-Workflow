# Prodige Workflow Pointer

Read and follow `../../AGENTS.md` before taking action.

Prodige is prompt-level. Load `../../.ai/boot/BOOT.md`, respect boundaries, and
route commands through `../../.ai/commands/registry.json`. Production deployment
execution is outside Prodige's boundary; prepare handoff artifacts only.
