# Prodige Workflow Pointer

Read and follow `../../AGENTS.md` before taking action.

Load `../../.ai/boot/BOOT.md` and use `../../.ai/scripts/prodige-mcp.js` when
MCP tools are available. Route slash commands through
`../../.ai/commands/registry.json` and respect all HITL gates.
