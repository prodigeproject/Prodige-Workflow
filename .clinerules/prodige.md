# Prodige Workflow Pointer

Read and follow `../AGENTS.md` before taking action.

Mandatory boot sequence:
1. Read `../AGENTS.md`.
2. Read `../.ai/boot/BOOT.md`.
3. Route slash commands through `../.ai/commands/registry.json`.
4. Use `../.ai/scripts/prodige-mcp.js` when MCP tools are available.

Do not edit code before loading context, planning, and respecting HITL gates.
